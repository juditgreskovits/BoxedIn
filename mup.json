{
  // Server authentication info
  "servers": [
    {
      "host": "109.74.198.222",
      "username": "juditgreskovits",
      // "password": "route66_2HeAveN",
      // or pem file (ssh based authentication)
      "pem": "~/.ssh/id_rsa"
    }
  ],

  // Install MongoDB in the server, does not destroy local MongoDB on future setup
  // "setupMongo": true,

  // WARNING: Node.js is required! Only skip if you already have Node.js installed on server.
  // "setupNode": true,

  // WARNING: If nodeVersion omitted will setup 0.10.31 by default. Do not use v, only version number.
  // "nodeVersion": "0.10.31",

  // Install PhantomJS in the server
  "setupPhantom": true,

  // Application name (No spaces)
  "appName": "JuditGreskovits",

  // Location of app (local directory)
  "app": "/Users/juditgreskovits/Projects/BoxedIn",

  // Configure environment
  "env": {
    "PORT": "3003",
    "ROOT_URL": "http://kapotasana.com",
    "METEOR_ENV": "production"
  },

  // Meteor Up checks if the app comes online just after the deployment
  // before mup checks that, it will wait for no. of seconds configured below
  "deployCheckWaitTime": 15
}
